package com.assistant.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Deep Space Theme ──
val BgDeep          = Color(0xFF0A0A0F)
val BgDark          = Color(0xFF0E0E18)
val SurfaceDark     = Color(0xFF12121E)
val CardDark        = Color(0xFF1A1A2C)
val CardLight       = Color(0xFF222236)

// ── Brand Colors ──
val PrimaryPurple   = Color(0xFF7C6EFA)
val PrimaryDark     = Color(0xFF5A4FD4)
val PrimaryGlow     = Color(0xFF9B8FFF)
val AccentTeal      = Color(0xFF06D6A0)
val AccentPink      = Color(0xFFFF6B9D)
val AccentGold      = Color(0xFFFFD166)
val AccentBlue      = Color(0xFF4FC3F7)

// ── Message Colors ──
val UserBubble      = Color(0xFF5A4FD4)
val AiBubble        = Color(0xFF1E1E32)
val AiBubbleStroke  = Color(0xFF7C6EFA)

// ── Text ──
val TextPrimary     = Color(0xFFF0F0FF)
val TextSecondary   = Color(0xFFBBBBDD)
val TextMuted       = Color(0xFF8888AA)
val TextDim         = Color(0xFF555577)

// ── Status ──
val ErrorRed        = Color(0xFFFF5C7A)
val SuccessGreen    = Color(0xFF06D6A0)
val WarnOrange      = Color(0xFFFFD166)
val InfoBlue        = Color(0xFF4FC3F7)

// ── Overlay ──
val OverlayBg       = Color(0xEA0A0A0F)
val OverlayStroke   = Color(0xFF2A2A44)

// ── Legacy aliases (keep compatibility) ──
val TextLight       = TextPrimary
val TextGray        = TextMuted